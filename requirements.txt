# ─────────────────────────────────────────────────────────────────────────────
# EduAlert Backend — Python Dependencies
# University of Energy and Natural Resources (UENR) · Group 27
# ─────────────────────────────────────────────────────────────────────────────
# Install:
#     pip install -r requirements.txt
#
# Virtual environment (recommended):
#     python -m venv venv
#     venv\Scripts\activate          (Windows)
#     source venv/bin/activate       (Mac / Linux)
#     pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Database (MongoDB) ────────────────────────────────────────────────────────
motor==3.3.2
pymongo==4.6.3

# ── Authentication & security ─────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Request handling ──────────────────────────────────────────────────────────
python-multipart==0.0.9
python-dotenv==1.0.1

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.6.4

# ── ML model serving ──────────────────────────────────────────────────────────
joblib==1.3.2
numpy==1.26.4
scikit-learn==1.4.2
pandas==2.2.1

# ── Email delivery (SendGrid — free tier: 100 emails/day) ─────────────────────
# Used for:  password reset codes, high-risk student alerts, weekly summaries
# Setup:     1. Create free account at https://sendgrid.com
#            2. Settings → API Keys → Create API Key (Full Access)
#            3. Add to .env:  SENDGRID_API_KEY=SG.xxxxxxxxxxxx
#            4. Add to .env:  EMAIL_FROM=noreply@yourdomain.com
#            5. Verify sender identity in SendGrid dashboard
sendgrid==6.11.0

# ── SHAP explainability (optional — for real per-prediction SHAP values) ──────
# Currently EduAlert uses a fast SHAP proxy formula.
# Uncomment this for real SHAP explanations (~2s added per prediction):
 shap==0.45.1